""""
Kullanıcıdan bir cümle al ve kaç tane "a" harfi geçtiğini bul.
cumle = input("Bir cumle giriniz: ")
print(cumle.count("a"))

Kullanıcının girdiği cümledeki boşluk karakterlerini "-" ile değiştir.
cumle = cumle.replace(" ", "-")

Girilen metni tamamen büyük harfe çevir.
metin = metin.upper()

Girilen metni tamamen küçük harfe çevir.
metin = metin.lower()

Kullanıcının adını al ve baş harfleri büyük olacak şekilde yazdır (title case).
ad = input("adınız: ")
print(ad.title())

Kullanıcının girdiği cümleyi kelimelere ayır ve liste olarak yazdır.
cumle = cumle.split()
print(cumle)

Kullanıcının girdiği cümledeki kelimeleri - karakteri ile birleştir.
cumle "-".join(cumle)

Bir cümle içinde geçen "Python" kelimesinin başlangıç index'ini bul.
print(cumle.find("Python))

Kullanıcının girdiği kelimeyi ortalayarak, toplam 30 karakterlik bir alan içinde yazdır (center).
kelime = input("Bir kelime yazınız: ")
print(kelime.center(30))

Sayı listesinin en büyük ve en küçük elemanını yazdır.
sayı = []
print(max(liste), min(liste))

Sayı listesini küçükten büyüğe sıralayıp ekrana yazdır.
sayı.sort
print(sayı)

Kullanıcının girdiği bir kelimenin karakter sayısını
kelime = input("Bir kelime giriniz: ")
print(len(kelime))
"""
# 1- Kullanıcıdan bir metin al ve bu metinde kaç tane "a" harfi geçtiğini yazdır.
metin = input("Bir metin giriniz: ")
print(metin.lower().count("a"))
# 2- Kullanıcının girdiği cümledeki boşluk karakterlerini "-" ile değiştir.
print(metin.replace(" ","-"))
# 3- Girilen metni tamamen büyük harfe çevir ve ekrana yazdır.
print(metin.upper())
# 4- Girilen metni tamamen küçük harfe çevir ve yazdır.
print(metin.lower())
# 5- Kullanıcının adını ve soyadını al, baş harfleri büyük olacak şekilde yazdır (title case).
ad = input("Adınız:")
soyad = input("Soyadınız: ")
ad_soyad = ad +" "+ soyad
print(ad_soyad.title())
# 6- Kullanıcıdan bir cümle al ve bu cümleyi kelimelere ayırarak bir listeye dönüştür.
cumle = input("Bir cümle giriniz: ")
cumle_list = cumle.split()
# 7- Cümledeki kelimeleri "-" ile birleştirip yazdır.
print("-".join(cumle_list))
# 8- Kullanıcıdan bir metin al ve içinde geçen "Python" kelimesinin başlangıç indeksini yazdır.
metin1 = input("Bir metin giriniz: ")
'''print(metin1.index("Python")) inde bulamaz ise hata verir'''
print(metin1.find("Python"))
# 9- Kullanıcının girdiği kelimeyi 30 karakterlik bir alan içinde ortalayarak yazdır.
kelime = input("Bir kelime giriniz: ")
print(kelime.center(30))
# 10- Kullanıcıdan virgülle ayrılmış sayılar al ve bu sayıların toplamını, ortalamasını yazdır.
sayı = input("İstediğiniz kadar virgülle ayrılmış sayılar giriniz: ")
sayı = [int(i.strip()) for i in sayı.split(",")]
toplam = sum(sayı)
ortalama = toplam / len(sayı)
print(f"toplam: {toplam}, ortalama:{ortalama}")
# 11- Girilen sayıların en büyüğünü ve en küçüğünü bul.
en_buyuk = max(sayı)
en_kucuk = min(sayı)
# 12- Sayıları küçükten büyüğe sıralayıp yazdır.
sayı_sıralı = sorted(sayı)
print(sayı_sıralı) 
# 13- Kullanıcının girdiği bir kelimenin uzunluğunu (karakter sayısını) yazdır.
kelime = input("Bir kelime giriniz: ")
print(len(kelime))
# 14- Kullanıcıdan bir cümle al ve ilk 5 karakterini yazdır.
cumle = input("Bir cümle giriniz: ")
print(cumle[:5])
# 15- Kullanıcının girdiği cümlenin son 10 karakterini yazdır.
print(cumle[-10:])
# 16- Girilen metni tersten yazdır.
print(cumle[::-1])
# 17- Kullanıcının adının sadece ilk iki harfini büyük harf olarak yazdır.
kullanıcı_ad = "Abdurrahman"
print(kullanıcı_ad[:2].upper() + kullanıcı_ad[2:])
# 18- Kullanıcıdan bir metin al ve içinde kaç tane "e" harfi olduğunu yazdır.
metin = input("Bir metin giriniz: ").lower()
print(metin.count("e"))
# 19- Kullanıcının yazdığı metindeki tüm noktalama işaretlerini boşlukla değiştir.
from string import punctuation
for c in punctuation:
    metin = metin.replace(c," ")
# 20- Girilen cümleyi baş harfleri büyük olacak şekilde (title) dönüştür.
cumle = input("Bir cümle giriniz: ")
cumle = cumle.title()
# 21- Kullanıcının adını ve soyadını küçük harflerle yazdır.
kullanıcı_ad = "Kasım"
kullanıcı_soyad = "Uzunefe"
print(f"{kullanıcı_ad.lower()} {kullanıcı_soyad.lower()}")
# 22- Kullanıcının yazdığı bir kelimenin ilk ve son harfini yazdır.
kelime = input("Bir kelime giriniz: ")
print(kelime[0], kelime[-1])
# 23- Bir cümlede geçen tüm kelimeleri ters sırayla yazdır (sondan başa doğru).
cumle = "Bir cümlede geçen tüm kelimeleri ters sırayla yazdır"
cumle1 = cumle.split()
print(" ".join(cumle1[::-1]))
# 24- Kullanıcıdan alınan bir cümlede kaç kelime olduğunu bul (split kullan).
cumle = input("Bir cümle giriniz: ")
cumle = cumle.split()
print(len(cumle))
# 25- Kullanıcının yazdığı metinde geçen ilk boşluğun index'ini bul.
metin = input("Bir metin giriniz: ")
print(metin.index(" "))
# 26- Kullanıcıdan 5 sayı al, bunları listeye ekle ve toplamlarını yazdır.
sayı = input("5 tane virgülle ayrılmış sayılar giriniz: ")
sayı = [ int(i.strip()) for i in sayı.split(",")]
print(sum(sayı))
# 27- Liste içindeki sayıların ortalamasını hesapla.
liste = [1,2,3,4,5]
ortalama = sum(liste) / len(liste)
# 28- Listenin en küçük ikinci elemanını bul.
liste = [1,2,3,4,5,6,23,25,36,25]
liste.sort()
print(liste[1])
# 29- Listeyi büyükten küçüğe sırala.
liste = sorted(liste, reverse= True)
# 30- Listedeki sayıların sadece tek olanlarını ekrana yazdır.
tek = []
for i in liste:
    if i % 2 == 1:
        tek.append(i)
print(tek)
# 31- Liste içindeki çift sayıların sayısını bul.
cift = []
for i in liste:
    if i % 2 == 0:
        cift.append(i)
print(cift)
# 32- Kullanıcının girdiği sayıların karelerini yeni bir liste olarak oluştur.
sayı = input("İstediğiniz kadar virgülle ayrılmış sayılar giriniz: ")
sayı = [int(i.strip()) for i in sayı.split(",")]
karesi = [i**2 for i in sayı]
# 33- Kullanıcının girdiği sayılardan kaç tanesi 10’dan büyük?
j = 0
for i in sayı:
    if i > 10:
        j += 1
print(j)







 [i for i in liste if i % 2 == 0]
 
  [int(i.strip()) for i in sayı.split(",")]















